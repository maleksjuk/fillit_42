{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "fillit",
            "type": "shell",
            "command": "gcc -g -o start *.c libft/libft.a",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}